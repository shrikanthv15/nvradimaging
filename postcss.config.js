export default {
    plugins: {
        '@tailwindcss/postcss': {},
        autoprefixer: {}, // autoprefixer is included in @tailwindcss/postcss? No, keeping it is fine but usually optional.
    },
}
